

const dividir = (x, y) => {
    return x / y;
}
//Segunda forma para quando é só uma linha
const multiplicar = (x, y) => x * y;

//Quando se tem só um parâmetro não precisa parênteses
const concatenar = sob => 'Maria' + sob;

console.log("resultado: " + dividir(555, 12));